# Reference results

These files contain the recorded results used in the main README.

They are included so the published values can be checked without reading old training logs.

Run:

```bash
python scripts/verify_reference_results.py
```

The original raw checkpoints and full result ZIP files are not bundled because they are much larger than these summaries. Every training command in the main README recreates the same output structure.
